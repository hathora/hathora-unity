# This Dockerfile is auto-generated by HathoraServerDeploy.cs

FROM ubuntu

# Copy the server build files into the container
COPY ./Build-Server .

# Run the Linux server in headless mode as a dedicated server
CMD ./Hathora-Unity-LinuxServer.x86_64 -mode server -batchmode -nographics
